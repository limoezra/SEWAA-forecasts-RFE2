Issues:

Port set_data_directory.sh to python for windows portability.

When a new date is selected the value of the "Valid:" menu is not maintained
if "Plot all valid times" is selected.

If the plug is pulled half way through a forecast, it needs to be deleted
before the forecast can run again. Solution: Write to a temporary file and
move it to the final file.
- Add a re-run forecasts flag

Include the model version in the GAN*.nc file name. Without breaking the
current output.

Switching between "Show words" and "Show percentages" messes up the layout with
histograms (on my laptop screen width).

If 6h forecasts display, switching to 24h forecasts, when there are none, doesn't display
an error.
